#pragma once

namespace ts3d {
    namespace TestCommon {
        const QString ROOT_TEST_DIR = "../ui/src/tests/testdata/";
        const QString HSF_FILE = ROOT_TEST_DIR + "bnc.hsf";
        const QString STL_FILE = ROOT_TEST_DIR + "coffeeCup.stl";
        const QString OBJ_FILE = ROOT_TEST_DIR + "al.obj";
        const QString HSF_WITH_PMI_FILE = ROOT_TEST_DIR + "crank_arm.hsf";
        const QString PRC_FILE = ROOT_TEST_DIR + "pmi_Sample.prc";
        const QString POINT_CLOUD_FILE = ROOT_TEST_DIR + "points.xyz";
        const QString UNKNOWN_TYPE_FILE = ROOT_TEST_DIR + "UnknownFileType.data";
    }
}